1. kernel global-store + loadWorkflow + projectWorkflowNames + 测试；根 vitest setup。
2. server workflowStoreForRequest + 路由 + 测试。
3. dashboard 类型 / schema / i18n / App / editor + App.test。
4. 门禁：vitest 全量、typecheck:web、test:web、design-scale、comments、build:web、build:server；重启 :18765。
5. spec：kernel workflow-track-branches、server workflow-branches-and-skill-files、dashboard component-guidelines；提交；finish-work。
