# 执行计划

1. 启动已构建后端并确认接口响应。
2. 创建隔离临时项目和临时 workflow。
3. 运行 CLI init/status/check/advance，保存完整输出。
4. 查询任务状态、workflow 定义和 artifact catalog，保存 JSON。
5. 清理临时项目、workflow 和服务。
6. 运行后端相关测试、执行 `git diff --check`，整理结论。
