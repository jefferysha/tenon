# Implement
1. `npm i @xyflow/react -w @tenon/dashboard-app`；`TwoColumns`。
2. `SkillFlow.tsx` + 纯函数测试；`skillWaves.ts` 精简。
3. `IoTable.tsx`；`StageEditorPane.tsx` 重写 + 测试更新。
4. `WorkflowNav.tsx` + 测试；`WorkflowView.tsx` 接线；删旧文件与 i18n 孤儿键，增新键（zh/en 对齐）。
5. `SkillComposer.tsx` 改用可编辑 SkillFlow；`useWorkflowEditor.setSkills`；`SkillComposer.test.tsx` 更新。
6. 门禁：typecheck:web、test:web、check:design-scale、check:comments、build:web；Playwright 目检；重启 :18765。
7. spec `component-guidelines.md` 更新；提交；finish-work。
