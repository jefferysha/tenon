# S 设计待补

P 裁决后必须写死：`product.dashboardTokenPath` 实际路径；localhost 写端点方法与路径白名单；signal 文件；Change lock 内 CLI writer；observation key 构成；脱敏字段；统一事件名（代码当前候选为 `pending-decision-self-approval-suspected`），并将 `hook` 纳入 channel 枚举或改用现有枚举值。
