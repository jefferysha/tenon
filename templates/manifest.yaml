# templates/manifest.yaml — lite 内核流程规则的单一真相源
#
# 蒸馏来源（老仓 workflow-plugin，严格只读参考）：
#   · phases        ← skills/pipeline/manifest.yaml:phases（order 1..7 的 7 个相位）
#   · transitions   ← skills/pipeline/scripts/manifest.py:_DEFAULT_TRANSITIONS
#                     （event 边折叠为 from→to 相位边：build-complete=build→verify、
#                      verify-fail=verify→build 构成 build⇄verify 双向；
#                      archived=archive→archive 终态自环）
#   · review_phases ← skills/pipeline/manifest.yaml:review_phases。
#                     ⚠️ 老内核欠账：state-transition.sh 的 review-gate 块曾硬编码
#                     explore/spec/verify、未读该字段（manifest.yaml:78-80 自己注明"仅半接线"）。
#                     本仓引擎（packages/kernel/src/flow）**真读本字段**——改这里即改行为，
#                     回归锚见 flow.test.ts「review_phases 单一真相源回归锚」。
#
# 解析器 = kernel 手写窄解析（packages/kernel/src/flow/manifest.ts，CONTRACT §1 禁 yaml npm 包），
# 仅支持本文件用到的子集：
#   · 顶层键 `key:` / `key: [inline, list]`
#   · 块序列：两空格缩进 `- item`
#   · transitions 小节：两空格缩进 `from: [to1, to2]`
#   · `#` 整行注释、行尾注释（空白 + #）、空行
# 其余 YAML 特性（引号/锚点/多行标量/嵌套 map）一律不支持，遇到即 fail-loud。

# —— 7 相位，声明顺序即相位顺序（回退边判定 = 目标序号 < 当前序号）——
phases:
  - open
  - explore
  - spec
  - build
  - verify
  - ship
  - archive

# —— 转换图：from: [合法目标...] ——
# build 的 requirements-changed → spec 与 verify-fail → build 都是受控回退边，
# phase_status=in_progress；修订后的目标相位证据必须重新 record/read 才能再次前进。
# archive: [archive] = 终态自环（老内核 archived 事件，phase_status=done、archived=true 由 CLI 层落）。
# 每个已声明相位必须在此有条目（可为 []），缺条目 = 结构错误，loadManifest fail-loud。
transitions:
  open: [explore]
  explore: [spec]
  spec: [build]
  build: [verify, spec]
  verify: [ship, build]
  ship: [archive]
  archive: [archive]

# —— review-gate 相位：完成这些相位、准备离开时必须取得 canonical approval receipt ——
# 引擎经 isReviewPhase() 暴露判定；此列表是唯一真相源。`tenon review request` 才写 v2
# hook marker（receipt 同时绑定离开的 exact event），进入相位本身不写 marker，因而不会锁住该相位的调研/规格/验证工作。
review_phases: [explore, spec, verify]

# Skill contract 的显式动作授权。该版本化 closed list 是 AFK admission 的唯一 Skill 层来源；
# 不从 mandatory_skills / recommended_skills 的 slot 或任何 prose 推断权限。
skill_action_authority:
  version: v1
  _all: [enter-afk]
  pm: [enter-afk]
  frontend: [enter-afk]
  backend: [enter-afk]
  free: []

# ══════════════════════════════════════════════════════════════════════════════
# 全派生面数据节（BACKLOG #18 / GOAL A1）——全部经 kernel loadManifest 真派生，零硬编码。
# 蒸馏来源：老仓 skills/pipeline/manifest.yaml + scripts/manifest.py（严格只读参考）。
# 改这里任一数据 → 派生随之变（单一真相源钉死，manifest-derive.test.ts 回归锚）。
# ══════════════════════════════════════════════════════════════════════════════

# Track 路由默认值的单一真相源是 kernel tracks/builtins；项目覆盖写入
# `.pipeline/tracks.yaml` 的 `policy_profile.routing`，manifest 不再镜像路由规则。

# —— Track matrix overlay 的 phase × track 强制 skill 表（老 evidence 派生；缺则 full preset [HARD] 阻断）——
# 键 = 'phase.track'；`_all` = 不分 track、对每 track 兜底（老 evidence 三级回退：per-track → _all → 空）。
# 这些表只描述 Track overlay：只有 `policy_profile.skills.matrix=true` 时才自动叠加到
# Workflow phase requirement。default Workflow 的 `tenon-<phase>` hard Skill 属于冻结的
# Workflow capability，不在这里重复声明；free/matrix=false 仍可由 artifact/AFK 的显式
# profile resolver 选择 phase + named allowlist，但不会因此重新开启自动 overlay。
# 所有默认 overlay token 均是本插件内置 skill；新用户不依赖另一宿主、npm 或第三方 marketplace。
# 消费方：resolver required/available/explicit 三投影、doctor 与 router 注入。
mandatory_skills:
  open._all: [openspec-propose]
  explore.pm: [brainstorming, grill-with-docs]
  explore.frontend: [openspec-explore, brainstorming, grill-with-docs]
  explore.backend: [openspec-explore, brainstorming, grill-with-docs, improve-codebase-architecture]
  explore.free: [brainstorming]
  spec.pm: [openspec-propose, brainstorming, writing-plans, grill-with-docs]
  spec.frontend: [openspec-propose, writing-plans]
  spec.backend: [openspec-propose, writing-plans]
  spec.free: [openspec-propose, writing-plans]
  build.pm: [prototype, frontend-design]
  build.frontend: [test-driven-development, frontend-design]
  build.backend: [writing-plans, test-driven-development]
  build.free: [writing-plans, test-driven-development]
  verify.pm: [browser-qa, web-design-guidelines, design-taste-frontend, verification-before-completion, handoff]
  verify.frontend: [verification-before-completion, e2e-testing, browser-qa, web-design-guidelines, design-taste-frontend]
  verify.backend: [verification-before-completion]
  verify.free: [verification-before-completion]
  ship.pm: [openspec-apply-change, to-spec, to-tickets]
  ship.frontend: [openspec-apply-change, openspec-archive-change, finishing-a-development-branch]
  ship.backend: [openspec-apply-change, finishing-a-development-branch]
  ship.free: [openspec-apply-change, finishing-a-development-branch]
  # archive 无强制 skill（归档不 gate skill）——不声明即空表

# —— phase × track 推荐 skill 表（老 evidence 派生；缺只 WARN、不阻断）——
recommended_skills:
  explore.pm: [tenon-researcher]
  explore.frontend: [search-first]
  explore.backend: [search-first]
  build.pm: [prototype, frontend-design, hallmark]
  build.frontend: [react-patterns, hallmark]

# —— breadcrumb prose（老 phases.<phase>.breadcrumb 派生；`|` 字面块标量，缩进 4 定块界）——
# 消费方：router hook #19 每轮被动注入活跃 change 当前相位的行动提示。派生就绪待消费。
# 末尾换行 rstrip（对齐老 CLI `bc.rstrip("\n")`）；内部换行/空行逐字保留。
breadcrumb:
  open: |
    立项中。使用 router 已选的 track（pm/frontend/backend，或用户显式选择的 free）创建独立 Change，产出 proposal.md + tasks.md 骨架，init canonical state；只有 dispatch 明确 unresolved/select 或用户点名自定义 workflow 时才请求选择。
    别跳到实现。常规模式骨架齐后交用户过目、用户说"继续"再 transition open→explore；已绑定持续授权时，仍须完成真实 OpenSpec 文档证据和 tenon check，再按既有 guard 继续，绝不跳过验证。
  explore: |
    调研 + 深度设计：research 先用 AskUserQuestion 多选调研维度 → 每维度并行 dispatch 隔离 sub-agent
    tenon-researcher（同消息内并行、各自落盘）→ brainstorming（必做）→ grill（必做，禁自判跳过）。
    backend 额外 improve-codebase-architecture 架构扫描（必做）；free 只运行 free profile，不叠加领域轨技能。任何 gap 用 AskUserQuestion 批量问、
    迭代到清零；硬取舍落 ADR。产出 design_doc（含 coverage 块）后停下等用户逐项确认，说"继续"才 transition。
  spec: |
    规格 + 实施计划：所有 track 先产出 OpenSpec delta spec + 可执行 plan；pm 额外产出用户旅程图。
    plan 两条必做：①首阶段 = tracer bullet 纵向打通端到端最小链路；②按上下文窗口拆 build 子阶段，
    plan 里显式标注子阶段边界 + 何处 /clear。产出 plan + 填齐 coverage 块后停下等确认，说"继续"才 transition。
  build: |
    实现（与 verify 是 barrier、不并行）。完成一个 task 立即把 tasks.md 的 [ ] 改 [x]。
    fe·be·free：TDD 完整红-绿-重构——先写测试 → 运行确认按预期失败(红) → 最小实现到绿 → 重构。
    build 内紧反馈循环（必做）：每 task 自跑类型检查 + 测试 + lint、读输出、失败当场自纠，绿了才算完成。
    fe 含 UI 改动额外过 frontend-design + web-design-guidelines + taste 评→修→复评循环，证据落 REVIEW.md。
    全 task 自测绿后 build-complete 冻结可复验 build_sha 基线：branch/worktree 为 Git HEAD，in-place 为工作区内容 SHA-256；之后才进 verify。verify 跑完停下等复核。
  verify: |
    标准轨验证。fe·be：同消息并行 dispatch 三轨——reviewer agent + e2e-runner + codex exec（同读冻结 build_sha 基线；in-place 不得把它当 Git revision）；free 只执行 free profile 的验证技能与 Workflow 门禁。
    verify 是对 build 已自测产物的独立对抗式二次把关，非替代 build 自测。
    fe 含 UI 改动额外视觉轨：web-design-guidelines + taste 对跑起来的 app 视觉审查，结论并入 verification_report。
    pm：跳过三轨，人工走查原型。产出 verification_report、set verify_result 后停下等复核；verify-fail → 回 build。
  ship: |
    发布。pm：产出 PRD（set prd_path）+ handoff。fe·be：sync spec + 建 PR（set pr_url）。free 按所选 Workflow 与 free profile 应用 spec，不补领域交付物。
    verify_result≠pass 不许 ship。ship 完停下，用户确认后再手动 transition 进 archive——不自动推进。
  archive: |
    归档 + 沉淀。用 tenon archive 归档（别手动改 .pipeline.yaml）。
    debug≥2 轮 / 比较≥2 方案 / 用户要求"沉淀" → 触发 learn-record；做不做把选项问用户拍板，别自行跳。
